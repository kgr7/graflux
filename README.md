# Simple Mosquitto/InfluxDB2/Grafana IoT Monitoring

Configure environment variables in a `.env` file then `docker-compose up -d`. Login to Grafana on http://localhost:3000 and configure Grafana. Done.
